{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install && curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y && source ~/.cargo/env && cargo install dioxus-cli",
  "devCommand": "dx serve --port $PORT",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
